# Final Report: Bounded Page-Family Discovery Subsystem

## Overview
The Bounded Page-Family Discovery Subsystem has been successfully implemented and verified. This subsystem provides a safe, deterministic, and scalable way to discover and catalog pages across target data sources (SoccerStats, Forebet) without performing a full, exhaustive site crawl.

**Note**: This represents a **bounded page-family discovery**, not an exhaustive site crawl. Safety limits (depth, page limits, circuit breakers) strictly cap execution to ensure compliance with rate limits and operational budgets.

## Test Coverage
- **Discovery Test Count**: 18
- **Actual Total Test Count**: 110
- **Overall Project Coverage**: 77% (with `src/soccer_factory/discovery/` at 90%+ for core routing logic, though CLI integration lowers the average slightly). All core crawler models, limits, and classifications are fully tested.

## Catalog Outputs

### SoccerStats
- **Seeds**: 8
- **Pages Discovered**: 35
- **Pages Fetched**: 8
- **Pages Parsed**: 8
- **Pages Failed**: 0
- **Restricted URLs**: 3 (matched invalid paths/extensions)
- **External URLs**: 4
- **Unknown URLs**: 0
- **Unmapped Fixture URLs**: 20 (Recorded as `not_attempted` with reason `no_fixture_mapping` because live mode was not run).
- **Network Requests**: 0
- **Families Found (15)**: `faq`, `form_table`, `generic_table`, `home_away`, `homepage`, `league_latest`, `league_view`, `legal`, `match_list`, `match_preview`, `matches`, `results`, `round_details`, `statistical_overview`, `wide_table`
- **Families Missing (5)**: `favourite_stats`, `leagueview_team`, `stats_by_month`, `team_stats`, `trends`
- **Stop Reason**: `None`

### Forebet
- **Seeds**: 5
- **Pages Discovered**: 18
- **Pages Fetched**: 5
- **Pages Parsed**: 5
- **Pages Failed**: 0
- **Restricted URLs**: 0
- **External URLs**: 0
- **Unknown URLs**: 0
- **Unmapped Fixture URLs**: 13 (Recorded as `not_attempted` with reason `no_fixture_mapping`).
- **Network Requests**: 0
- **Families Found (17)**: `asian_handicap`, `btts`, `by_country`, `cards`, `corners`, `daily_predictions`, `double_chance`, `finished_predictions`, `goals_market`, `goalscorers`, `half_time`, `half_time_full_time`, `live_predictions`, `result_market`, `tomorrow_predictions`, `top_predictions`, `weekend_predictions`
- **Families Missing (3)**: `by_league`, `prediction_list`, `values_or_odds`
- **Stop Reason**: `None`

## System Guarantees
- **Append-only behavior**: The catalog `CatalogStore` is strictly append-only.
- **Deduplication**: Snapshots are keyed by `content_hash`. Repetitive identical hashes do not overwrite old data; if the hash changes, a new snapshot row is added. If the exact same URL + content hash arrives, it is appended as a distinct observation with a unique timestamp in the `catalog.jsonl` log, preserving a comprehensive history. `representatives.jsonl` tracks the most recent valid fetch per family to use for parsers.
- **Network isolation**: The fixture mode successfully mapped its URLs exactly to local stubs and prevented any rogue HTTP requests. Live discovery was not run.
- **Immutability of previous pipeline**: The existing pipeline (feature generation, grading, validation) was verified against the new code. Artifacts remain identical (4 canonical market predictions, 16 no-predictions generated by features).

## Live Discovery Status
Live discovery has **not** been run. The current results were generated exclusively via fixture mode to guarantee safety. The system is structurally prepared for live validation pending user authorization.
